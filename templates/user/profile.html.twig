{% extends 'base.html.twig' %}

{% block header_img %}{% endblock %}

{% block header_title %}<h1>Mon profil</h1>{% endblock %}

{% block body %}

    <div class=" col-xs-12 col-sm-6 col-md-6 col-lg-4 col-xl-4">

        <div class="row profileCard">
            <p><strong>Nom :</strong> {{ userSession.surname }}</p>
            <p><strong>Prénom :</strong> {{ userSession.firstname }}</p>
            <p><strong>Email :</strong> {{ userSession.email }}</p>
        </div>

        <br/>

        <div class="row">
            <a href="{{ path('userPassword_edit', {'id' : userSession.id}) }}" class="btn btn-danger">Modifier mon mot de passe</a>
            <a href="{{ path('user_edit', {'id' : userSession.id}) }}" class="btn btn-success pull-right">Editer mon profil</a>
        </div>
    </div>

{% endblock %}